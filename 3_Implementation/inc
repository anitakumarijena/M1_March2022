#ifndef _MYFUNCH_
#define _MYFUNCH_

/**
 * @brief it is a password protected diary
 * 
 */
int password();

/**
 * @brief it take the password imput from user
 * 
 */
void addRecord();

/**
 * @brief itadd record to the diary database.
 * 
 */
void viewRecord();

/**
 * @brief it allow to view the records.
 * 
 */
void editRecord();

/**
 * @brief it will change the record data.
 * 
 */
void edit_password();

/**
 * @brief it allow to change the password.
 * 
 */
void deleteRecord();

/**
 * @brief it will delete th record.
 * 
 */
void Decision_Statements();

/**
 * @brief it will print the contents of loops.
 * 
 */
void Loops();

/**
 * @brief it will print the contents .
 * 
 */

void Suggestion();

int my_test(int a, int b);
int my_test(int a, int b){
	return a+b;
}

#endif
